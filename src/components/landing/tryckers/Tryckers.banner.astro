---
import { Icon } from "astro-icon"
import Button from "../../global/ui/button.astro"

const info = {
  title: "Tryckers",
  subtitle:
    "Nuestra comunidad está formada por personas apasionadas por la tecnología, el aprendizaje continuo y la colaboración. ¡Conoce a quienes hacen parte de esta red que impulsa el crecimiento en LATAM!",
  features: [
    {
      icon: "🌎",
      title: "Diversidad LATAM",
      description: "Conecta con personas de diferentes países y trayectorias.",
    },
    {
      icon: "🤝",
      title: "Colaboración real",
      description: "Participa en proyectos open source, talleres y mentorías.",
    },
    {
      icon: "🚀",
      title: "Crecimiento continuo",
      description: "Aprende sobre tech, soft skills y oportunidades laborales.",
    },
  ],
}
---

<section>
  <div class="px-4 lg:mx-auto lg:max-w-screen-xl">
    <div
      class="bg-tc-black border border-gray-700 rounded-lg p-10 lg:p-12 mb-8"
    >
      <!-- Título -->
      <div
        class="flex items-center justify-center text-tc-gray-l text-3xl lg:text-4xl font-extrabold mb-4"
      >
        <Icon class="w-12 h-12 text-white" name="raphael:people" />
        <span class="ml-3">{info.title}</span>
      </div>

      <!-- Subtítulo -->
      <p class="text-center text-gray-300 max-w-3xl mx-auto text-lg mb-8">
        {info.subtitle}
      </p>

      <!-- Características / Beneficios -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8 max-w-5xl mx-auto">
        {
          info.features.map((item) => (
            <div class="bg-tc-black-l border border-gray-700 hover:shadow-2xl transition-transform transform hover:scale-105 rounded-md p-6 text-center">
              <div class="text-3xl mb-2">{item.icon}</div>
              <h3 class="text-tc-yellow font-bold text-lg mb-1">
                {item.title}
              </h3>
              <p class="text-gray-400 text-sm">{item.description}</p>
            </div>
          ))
        }
      </div>

      <!-- Enlace Tryckers -->
      <Button
        buttonText="Conoce a los Tryckers de la comunidad"
        buttonLink="/tryckers"
      />
    </div>
  </div>
</section>
